
  $('.lecturects-group').flickity({
    // options
    cellAlign: 'left',
    contain: true,
    wrapAround:true,
    autoPlay:5000,
  });
  $('.students-group').flickity({
    // options
    cellAlign: 'left',
    contain: true,
    wrapAround:true,
    autoPlay:5000,
  });
  